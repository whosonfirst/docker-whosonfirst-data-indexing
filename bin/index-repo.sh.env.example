# See also index.sh.env.example

ECS_CONTAINER="${ECS_CONTAINER:=}"

ECS_CLUSTER="${ECS_CLUSTER:=}"

ECS_TASK="${ECS_TASK:=}"

ECS_TASK_VERSION="${ECS_VERSION:=}"

# for example '-subnet subnet-{FOO} -subnet subnet-{BAR}'
ECS_SUBNETS="${ECS_SUBNETS:=}"

ECS_SECURITY_GROUP="${ECS_SECURITY_GROUP:=}"

ECS_DSN="${ECS_DSN:=}"