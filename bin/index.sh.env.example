INDEX_ALL="${INDEX_ALL:=}"
INDEX_MYSQL="${INDEX_MYSQL:=}"
INDEX_ELASTICSEARCH="${INDEX_ELASTICSEARCH:=}"
INDEX_S3="${INDEX_S3:=}"

ES_HOST="${ES_HOST:=}"
ES_INDEX="${ES_INDEX:=}"

MYSQL_HOST="${MYSQL_HOST:=}"
MYSQL_DATABASE="${MYSQL_DATABASE:=}"
MYSQL_CREDENTIALS="${MYSQL_CREDENTIALS:=}"

S3_DSN="${S3_DSN:=}"

INDEX_ALL="${INDEX_ALL:=}"
INDEX_MYSQL="${INDEX_MYSQL:=}"
INDEX_ELASTICSEARCH="${INDEX_ELASTICSEARCH:=}"
INDEX_S3="${INDEX_S3:=}"

WORKDIR="${WORKDIR:=/usr/local/data}"

GITHUB_ORG="${GITHUB_ORG:=whosonfirst-data}"
GITHUB_TOKEN=""
