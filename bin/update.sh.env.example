UPDATED_SINCE="${UPDATED_SINCE:=P7D}"

ECS_DSN="${ECS_DSN:=credentials=iam: region=us-east-1}"

ECS_TASK="${ECS_TASK:=whosonfirst-data-indexing:N}"
ECS_CONTAINER="${ECS_DSN:=whosonfirst-data-indexing}"
ECS_CLUSTER="${ECS_CLUSTER:=whosonfirst}"

ECS_SECURITY_GROUP="${ECS_SECURITY_GROUP:=}"
ECS_SUBNET="${ECS_SUBNET:=}"

ECS_COMMAND="${ECS_DSN:=/usr/bin/index.sh -R -r}"